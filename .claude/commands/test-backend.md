Run the backend test suite and report results.

```bash
cd /Users/ianroybal/cooking-chatbot/backend && uv run pytest -v
```

If any tests fail, analyze the failure and fix the issue. Then re-run the tests to confirm the fix.
